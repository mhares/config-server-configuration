server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistry: false # Never fetch for the other microservices as eurekaServer is not going to call any of them
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
